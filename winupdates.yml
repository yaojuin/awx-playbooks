---
- hosts: '{{ target }}'
  tasks:
  - name: Install all critical and security updates
    win_updates: 
      category_names: 
      - CriticalUpdates
      - SecurityUpdates
      - FeaturePacks
      - UpdateRollups
      - DefinitionUpdates
      state: installed
      reboot: yes
    
